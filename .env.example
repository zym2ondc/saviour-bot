# --- REQUIRED ---
DISCORD_TOKEN=paste-your-bot-token-here
CLIENT_ID=paste-your-application-client-id-here
# Optional: single server ID for testing. Leave EMPTY = bot auto-deploys instantly to every server it's in (no GUILD_ID needed).
GUILD_ID=paste-your-server-id-here

# --- OPTIONAL (ticket config) ---
# Category ID where ticket channels get created. Right-click category -> Copy ID. Leave empty = creates at top.
TICKET_CATEGORY_ID=
# Staff role ID that can see all tickets. Leave empty = only user + admins can see.
TICKET_STAFF_ROLE_ID=
# Log channel ID where bot posts "ticket created/closed". Leave empty = no logs.
TICKET_LOG_CHANNEL_ID=

# --- VERIFY / RESTORE (OAuth2) ---
# Get from Discord Developer Portal -> your app -> OAuth2 -> Client Secret (Reset Secret to see it)
CLIENT_SECRET=paste-client-secret-here
# Must EXACTLY match a Redirect URL you add in Portal -> OAuth2 -> Redirects
# For local test: http://localhost:3000/callback
# For real use (members on other PCs): https://your-public-url/callback  (use ngrok / Railway / VPS)
OAUTH_REDIRECT_URI=http://localhost:3000/callback
PORT=3000
# Optional role given after verify (right-click role -> Copy ID)
VERIFIED_ROLE_ID=
UNVERIFIED_ROLE_ID=

# --- MUSIC (optional) ---
# If YouTube blocks playback ("Sign in to confirm you're not a bot"), paste a YouTube cookie string here.
YT_COOKIE=

# --- TIKTOK AUTO-CHECK (optional, only needed for /media setup + /media check) ---
# The free TikTok lookup (TikWM) Cloudflare-blocks most VPS IPs, which breaks
# auto-detection with "blocked from this server". Fix (2 min, free):
#   1. ON THE VPS (same IP the bot runs on), open Chrome and visit:
#        https://www.tikwm.com/api/user/posts?unique_id=tiktok&count=1
#   2. If you see "Just a moment..." tick the box / wait until JSON (or the
#      TikWM site) loads.
#   3. Press F12 -> Application tab -> Cookies -> https://www.tikwm.com ->
#      copy the VALUE of "cf_clearance" (long string, no "cf_clearance=" part).
#   4. Paste it below and restart the bot. Then test with /media check.
# If the cookie expires (weeks/months) or the VPS IP changes, repeat once.
# Leave empty = auto-check will fail with a clear error; /media post still works.
TIKWM_COOKIE=
